[build]
  base = "frontend"
  command = "npm run build"
  functions = "./netlify/functions"
  publish = "dist"

[[redirects]]
  from = "/api/*"
  to = "${BACKEND_URL}/api/:splat"
  status = 200
  force = true
