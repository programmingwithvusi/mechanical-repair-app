[build]
  base = "frontend"
  command = "npm run build --prefix backend && npm run build --prefix frontend"
  functions = "./netlify/functions"
  publish = "frontend/dist"

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/server/*." 
  status = 200
  force = true
